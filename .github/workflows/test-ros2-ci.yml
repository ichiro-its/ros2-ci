name: Test ROS 2 CI
on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master
jobs:
  test:
    runs-on: ubuntu-latest
    name: Test the ROS 2 CI
    steps:
      - name: Checking out
        uses: actions/checkout@v2.3.4
      - name: Test the ROS 2 CI
        uses: ./
